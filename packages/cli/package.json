{
  "name": "@agent-ready/cli",
  "version": "0.1.0",
  "description": "Command-line interface for Agent-Ready Schema.",
  "type": "module",
  "bin": {
    "ars": "./dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "lint": "tsc --noEmit",
    "clean": "rm -rf dist *.tsbuildinfo",
    "prepublishOnly": "npm run build"
  },
  "author": "Diego Souza <DiegoSouzadsp> (Orquestra AI)",
  "license": "MIT",
  "dependencies": {
    "@agent-ready/core": "*",
    "commander": "^12.1.0",
    "picocolors": "^1.1.1"
  },
  "devDependencies": {
    "@types/node": "^22.15.0"
  },
  "engines": {
    "node": ">=18"
  }
}
